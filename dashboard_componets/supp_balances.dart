import 'package:flutter/material.dart';



class Supp_balance extends StatelessWidget {
  const Supp_balance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Balance"),
      ),
    );
  }
}
