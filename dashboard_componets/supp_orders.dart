import 'package:flutter/material.dart';



class Supp_orders extends StatelessWidget {
  const Supp_orders({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Orders"),
      ),
    );
  }
}
