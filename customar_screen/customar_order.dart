import 'package:flutter/material.dart';



class Customar_order extends StatelessWidget {
  const Customar_order({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Customar Order"),
      ),
    );
  }
}
